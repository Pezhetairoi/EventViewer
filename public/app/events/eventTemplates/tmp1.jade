table.table.table-hover.table-striped.table-condensed
   thead
      tr
         th Title
         th Location
         th Date
      tbody
         tr(ng-repeat="event in events | filter:searchText | orderBy:sortOrder")
            td
               a(href="/events/{{event._id}}") {{event.title}}

            td {{event.address}}, &nbsp; {{event.city}} {{event.state}}
            td {{event.date | date}}

